[project]
name = "skive-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fpdf2>=2.8.8",
    "langchain>=1.4.2",
    "langchain-openai>=1.6.2",
    "langgraph>=1.2.11",
    "pypdf>=6.19.0",
    "python-dotenv>=1.2.3",
    "python-pptx>=1.0.2",
]

[project.scripts]
skive-practice = "skive_practice:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
